# راهنمای راه‌اندازی کامل

## 🏗️ معماری نهایی (و چرا اینطوریه)

| بخش | با چی ساخته شده | هزینه |
|---|---|---|
| لیست بازی‌ها و تنظیمات | Firestore Database | ✅ رایگان، بدون کارت |
| لاگین پنل مدیریت | Firebase Authentication | ✅ رایگان، بدون کارت |
| **عکس بازی‌ها** | یک ریپازیتوری عمومی GitHub | ✅ رایگان، بدون کارت |
| ساخت خودکار APK | GitHub Actions | ✅ رایگان، بدون کارت |

⚠️ **نکته مهم:** قبلاً گفته بودم Firebase Storage برای عکس‌ها رایگانه. این دیگه درست نیست — از فوریه ۲۰۲۶ گوگل شرط کرده که برای استفاده از Storage باید کارت بانکی (پلن Blaze) وصل کنی، حتی اگه هیچی ازت کم نشه. برای اینکه واقعاً به هیچ کارتی نیاز نداشته باشی، عکس‌ها رو به‌جاش روی خود GitHub (که حسابشو داری) نگه می‌داریم. نتیجه یکیه، هزینه صفره.

---

## بخش ۱ — ساخت پروژه Firebase

1. برو به [console.firebase.google.com](https://console.firebase.google.com)
2. **Add project** → یک اسم بذار (مثلاً `game-settings`)
3. Google Analytics رو می‌تونی خاموش کنی
4. **Create Project**

## بخش ۲ — فعال کردن Firestore Database

1. از منوی سمت راست: **Build → Firestore Database → Create Database**
2. حالت **Production mode** رو انتخاب کن
3. یک Location نزدیک انتخاب کن (فرقی نمی‌کنه زیاد)
4. بعد از ساخته شدن، برو به تب **Rules**
5. محتوای فایل `firestore.rules` (که براتون ساختم) رو کپی و جایگزین کن، بعد **Publish**

## بخش ۳ — فعال کردن Authentication (فقط برای دوستت)

1. **Build → Authentication → Get Started**
2. تب **Sign-in method** → **Email/Password** → فعالش کن
3. تب **Users** → **Add user**
4. یک ایمیل و رمز عبور بساز — این می‌شه اطلاعات ورود دوستت به پنل مدیریت

## بخش ۴ — گرفتن Config برای اپ اندروید

1. ⚙️ **Project Settings** (بالا سمت راست) → پایین صفحه بخش **Your apps**
2. آیکون **Android** رو بزن
3. Package name رو دقیقاً همینو بزن: `com.gamesettings.app`
4. **Register app** → **Download google-services.json**
5. این فایل رو داخل پوشه `app/` پروژه بذار (کنار `build.gradle.kts` داخل app) — قبل از آپلود به گیت‌هاب

## بخش ۵ — گرفتن Config برای پنل مدیریت (وب)

1. همون Project Settings → Your apps
2. این بار آیکون وب `</>`
3. یک اسم بده → **Register app**
4. یک بلوک کد `firebaseConfig` نشونت می‌ده — این ۶ مقدار رو کپی کن
5. فایل `index.html` (پنل مدیریت) رو با یک ویرایشگر متن (حتی Notepad) باز کن
6. بالای فایل، بخش `firebaseConfig` رو با مقادیر واقعی خودت جایگزین کن

## بخش ۶ — ساخت دو ریپازیتوری GitHub

**۱. ریپازیتوری کد اپ (خصوصی):**
github.com → **New repository** → اسم `game-settings-app` → **Private** → Create

**۲. ریپازیتوری عکس‌ها (باید Public باشه):**
github.com → **New repository** → اسم `game-images` → **Public** → Create
(عمومی بودن لازمه چون قراره لینک عکس‌ها مستقیم توی اپ لود بشه)

## بخش ۷ — ساخت توکن GitHub (برای آپلود عکس از پنل)

1. GitHub → عکس پروفایل → **Settings** → پایین صفحه **Developer settings**
2. **Personal access tokens → Fine-grained tokens → Generate new token**
3. یک اسم بده
4. **Repository access → Only select repositories** → فقط `game-images` رو تیک بزن
5. **Permissions → Contents → Read and write**
6. **Generate token** → کپی کن (فقط یک‌بار نشون داده می‌شه!)
7. توی `index.html`، مقدار `GITHUB_TOKEN` رو با همین پر کن؛ `GITHUB_OWNER` رو هم با یوزرنیم گیت‌هابت

⚠️ این توکن رو با کسی شریک نکن. چون فقط به یک ریپازیتوری (که فقط عکسه) دسترسی داره، حتی اگه لو بره بدترین حالت اینه که یکی عکس مزاحم آپلود کنه، نه بیشتر.

## بخش ۸ — آپلود کد اپ به GitHub (بدون نیاز به دستور)

1. برو داخل ریپازیتوری `game-settings-app`
2. لینک **uploading an existing file** رو بزن
3. تمام فایل‌ها/پوشه‌های پروژه (که ساختم، همراه `google-services.json` که خودت اضافه کردی) رو بکش و ول کن
4. **Commit changes**

همین که Commit بزنی، GitHub Actions خودش شروع می‌کنه به ساختن APK — نیازی به هیچ کار دیگه‌ای نیست.

## بخش ۹ — دانلود APK آماده

1. تب **Actions** بالای ریپازیتوری رو باز کن، صبر کن (۲–۴ دقیقه) تا ✅ بخوره
2. یا ساده‌تر: تب **Releases** (سمت راست صفحه اصلی ریپو) → APK همونجا برای دانلوده
3. فایل رو به گوشی منتقل کن و نصب کن (باید اجازه نصب از منابع ناشناس رو بدی)

از این به بعد، هر وقت دوستت بازی جدید اضافه کنه، فقط کافیه توی اپ دکمه **به‌روزرسانی** بالای صفحه رو بزنی — نیازی به نصب دوباره نیست.

## بخش ۱۰ — استفاده دوستت از پنل مدیریت

فایل `index.html` رو براش بفرست. فقط باید دابل‌کلیک کنه و توی هر مرورگری باز می‌شه (نیازی به هاست کردن نیست):
- با ایمیل/رمزی که بخش ۳ ساختی وارد بشه
- بازی اضافه/ویرایش/حذف کنه، عکس آپلود کنه

اگه بخوای یک لینک آنلاین هم برای این پنل داشته باشی (مثلاً برای اینکه از گوشی هم قابل بازکردنه)، می‌شه رایگان روی GitHub Pages گذاشتش — بگو تا برات انجامش بدم.

---

## فایل‌های این پروژه

```
GameSettingsApp/          → کد اپ اندروید (آپلود به ریپازیتوری خصوصی)
├── app/                  → (google-services.json رو خودت اینجا اضافه کن)
├── .github/workflows/    → دستور ساخت خودکار APK
└── firestore.rules       → قوانین امنیتی (Copy/Paste توی Firebase Console)

AdminDashboard/
└── index.html            → پنل مدیریت (برای دوستت)
```
